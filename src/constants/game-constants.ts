export const stepInterval = 20;

export enum ComponentTypes {
    ASTEROID = 'asteroid',
    BULLET = 'bullet',
    LIVES_COUNTER = 'lives-counter',
    PLAYER = 'player',
    SPACESHIP = 'spaceship',
    TEXT = 'text',
}
